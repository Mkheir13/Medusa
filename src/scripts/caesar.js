const input = document.querySelector('.input');

input.addEventListener('input', (e) => {
    if (e.target.value === "yodksk loinokx") {
        console.log("You found the secret message!");
    }
});
